\section{What the blind curve hid}
\label{sec:calibration}

Three acceptance criteria were fixed in a design document before any sweep ran:
a detectable halving of per-cell output, $s = 0.5$; interval coverage of 90\%;
and discrimination of 80\%, the probability the interval excludes zero at that
effect.\iffull{} The cutpoints follow mechanically --- the smallest $n$ meeting
both criteria, and the smallest meeting coverage alone.\fi The pre-registration is
mechanical rather than documentary: the panel, the labelling axes and the
falsification rule are frozen in a configuration file
\iffull
with a \texttt{frozen\_on} stamp and mirrored by an exact-value assertion in the
test suite, so continuous integration fails if anyone edits one without the
other.
\else
and asserted in the test suite, so continuous integration fails if anyone edits
one without the other.
\fi

Run properly on real held-out cells, the committed cutpoint of 50 does not come
back\iffull{} (Figure~\ref{fig:calibration})\fi. The generator filters eligibility on
patient alone, so both synthetic arms draw from the held-out patients' cells of
\emph{both} tissues pooled.
\iffull
The design document lists what stays fixed and never
mentions tissue. Nobody made that choice; an implementation detail made it, and
it changes the answer.
\else
The design document never mentions tissue: nobody made
that choice, an implementation detail made it, and it changes the answer.
\fi
So we report both readings rather than the one that
yields a cutpoint. Pooled, the routine returns no cutpoint at all: discrimination
never reaches the 0.80 target in any count bin, topping out at 0.78 across 13
seeds at 50 replicates and at 0.75 across 8 seeds at 500. Restricted to the
reference tissue it does return one, and that is where the second lesson of this
paper repeats itself.

\iffull
\begin{figure}[t]
\centering
\iffull
\includegraphics[width=\linewidth]{figures/fig1_calibration.pdf}
\else
\includegraphics[width=0.88\linewidth]{figures/fig1_calibration.pdf}
\fi
\iffull
\caption{\textbf{The committed cutpoint is not what the pre-registered procedure
returns, under either reading of an unspecified design choice.} Dashed lines mark
the targets fixed before the sweep. Shading marks the committed rule's three
states. \emph{Left:} with the draw pool our sweep code implements, the routine
returns no cutpoint at any tested count. \emph{Right:} under a second reading of
the same unspecified choice it does return one --- but the value is the grid's,
not the cohort's: $100$, $90$ and $70$ on three grids of increasing resolution,
against a committed 50 that none of them returns.}
\else
\caption{\textbf{The committed cutpoint is not what the pre-registered procedure
returns, under either reading of an unspecified design choice.} Dashed lines are
the targets fixed before the sweep; shading marks the committed rule's three
states; the committed value is 50 in both panels.}
\fi
\label{fig:calibration}
\end{figure}
\fi


\iffull\else
\paragraph{The grid was reporting its own resolution.} The committed grid varies
a mature \emph{fraction} against a fixed 2{,}000 cells, so it can reach no count
between 40 and 100, and returns $ok = 100$ on every seed. Adding nine fractions
inside that gap and changing nothing else moves the answer to 90, against a
committed 50: the crossing it was hiding runs $0.728$ at 45 mature cells,
$0.789$ at 65 and $0.858$ at 90. A grid that cannot resolve a crossing still
returns a stable number, which is \S\ref{sec:blind}'s failure in a second
statistic.
\fi
\iffull
\paragraph{The grid was reporting its own resolution.} Our committed grid varies
a mature \emph{fraction} against a fixed 2{,}000 cells, so the counts it can
reach are $\{0, 20, 40, 100, 200, 400, 800\}$ --- nothing between 40 and 100. On
the reference pool it returns $ok = 100$ on every one of the eight seeds at 500
replicates\iffull{}, and on twelve of thirteen at 50,\fi{} and that stability is
what made the number look like a measurement.

\iffull
The superseded internal report gave 120, and we cannot reproduce it: 120 is not
a count the committed grid can reach. Whatever produced it was not the committed
configuration, and the run is the uncommitted one of
Appendix~\ref{app:withdrawn} item~3, so there is nothing left to re-derive it
from. We report that rather than reconcile it.
\fi

\textbf{And the reported cutpoint is not a count. It is a bin label.} Counts are
binned into twelve log-spaced intervals before coverage and discrimination are
computed, and the value reported for a bin is the \emph{median of the counts
that landed in it}. Over $20$--$800$ the edges are $\{0, 20, 28, 39, 55, 76,
107, 150, 209, 293, 409, 572, 800\}$, and they are identical for every grid
because they are derived from the same span. Nothing in the output says so, and
we did not know it while writing the paragraph above.

We added nine fractions inside the gap, reaching 50, 60, 70, 80, 90, 110, 120,
130 and 150, and changed nothing else. The answer moves to 90 --- and it moves
\emph{within one bin}. Both grids return the interval $(76, 107]$; the committed
grid held only $\{100\}$ there and the filled grid holds $\{80, 90, 100\}$, so
the median fell from 100 to 90 while the bin, and the evidence in it, stayed
put. \textbf{At the resolution of the statistic that produced it, the answer did
not move at all.} What the filled grid did buy is a genuinely new bin at
$(55, 76]$, where discrimination reads $0.790$ --- under target, so the crossing
lies above it.

\textbf{A third grid, and the number keeps falling.} A denser grid reaching
below 20 --- attainable counts $\{2.5, 10, 17.5, 27.5, 42.5, 70, 105, 150, 400,
800\}$ --- returns $ok = 70$ on eight of eight seeds. Discrimination against
count is smooth, and the three grids agree on it wherever they overlap
($0.712$, $0.728$, $0.728$ near 40; $0.882$, $0.858$, $0.878$ near 100). What
differs is only which point each grid happens to carry first above the $0.80$
line:

\begin{center}\small
\begin{tabular}{@{}lccc@{}}
\toprule
 & \textbf{committed} & \textbf{filled} & \textbf{dense} \
\midrule
crossing bracketed to & $(40, 100]$ & $(65, 90]$ & $(42.5, 70]$ \
reported $ok$ & $100$ & $90$ & $\mathbf{70}$ \
reported $wide$ & $40$ & $45$ & $42$ \
\bottomrule
\end{tabular}
\end{center}

\textbf{The reported cutpoint is a decreasing function of grid resolution, with
no sign of converging, and no grid, cohort or seed has ever returned the
committed 50.} Each value is the first point above a crossing, reported as
though it were the crossing. Within any one grid $wide$ has \emph{exactly zero}
seed-to-seed variance --- one value on all eight seeds, in all six
cohort-by-grid cells we ran ($40$, $45$, $42$ here; $100$, $65$, $70$ on the
second cohort) --- so the quantity that looks most like a measurement is the one
whose stability comes entirely from having nowhere else to land. The pooled draw
returns no cutpoint under any of the three grids, on any seed of either cohort,
discrimination topping out at $0.750$ and $0.795$ against the $0.80$ target, so
the non-identifiability there is not a resolution artefact.

\textbf{We do not report a calibrated cutpoint, because the procedure does not
return one.} It returns its own grid, and it did so with a stability that
survived three grids, thirteen seeds and two cohorts without once warning us.
\fi

\iffull
\paragraph{And none of the three is a cutpoint with an interval on it.} At 50
replicates the crossing does not resolve at all --- the returned value wanders
between 45 and 90 with the seed. At 500 it does, 7 of 8 seeds returning
$ok = 90$, but that stabilises Monte Carlo noise and not the quantity a reader
will take it for. Ten
patients with two held out per replicate admit just \textbf{45 distinct holdout
pairs}, so at 500 replicates each pair is drawn about eleven times and the
estimate converges to the value over those 45 pairs rather than to a population
value. The patient-level uncertainty is unchanged by the replicate count and is
not quantified anywhere in this paper. Read each value as what this procedure
returns on this cohort at that grid resolution. Reporting any of them with an
interval would be the same error this paper documents elsewhere, one section
later --- and \S\ref{sec:interval} shows the interval we would have reached
for is itself narrower than it claims at exactly these $n$.
\fi

\iffull
\paragraph{A second cohort returns a different number, and the same verdict.}
The two Lee cohorts are both local and both sha256-verified, and only one had
ever been run. On the second --- six patients against ten --- the pooled draw
again returns \emph{no} cutpoint, on every seed at both replicate counts: no
seed meets both criteria, and discrimination alone reaches the $0.80$ target at
50 replicates but tops out at $0.66$ at 500. The reference draw returns a
cutpoint, and it is nowhere near the committed 50: $ok = 400$ or $800$ on both
grids, the top two counts either can express --- and the top point of a grid is
the same artefact as the first point above a crossing, one edge further out.
\textbf{The pre-registered procedure returns a cutpoint between $1.4$ and $2$
times the committed one on the first cohort depending only on grid resolution,
and eight to sixteen times it on the second},
which is a statement about the procedure rather than about either cohort. The
blindness, being algebraic, replicates exactly: the residual is $0$ on all
$1{,}711{,}200$ replicates across both.

The instability is worse than the first cohort's, not better. At 50 replicates
the second cohort returns $ok = 800$ on eleven of thirteen seeds; at 500 it
returns $400$ on six of eight. \textbf{The majority answer moved when we added
replicates}, which is what Appendix~\ref{app:limitations} predicts --- more
replicates buy Monte Carlo precision, not patient-level precision, and six
patients admit fifteen distinct holdout pairs against the first cohort's
forty-five. The top grid point also asks for 800 mature cells per arm from a
pool holding 844, so that end of the grid is at the edge of what the cohort can
express in distinct cells; we report the number and the edge together.
\fi

\iffull
\paragraph{Three more of this paper's statistics could not fail, and one of
them is the interval.} Two grid caveats, the abstention-rate reading, three
withdrawn guards and the percentile bootstrap every result here rests on are
carried in Appendices~\ref{app:gridcaveats}--\ref{app:guards} rather than
inline. The one to read first is \S\ref{sec:interval}: the interval is
$0.82\times$ the width it claims at $n = 10$, by a closed form containing no
data, which makes it the same failure as this section's grid one layer out ---
and it is the layer every number in this paper passes through.
\fi

